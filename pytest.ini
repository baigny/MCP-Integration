[pytest]
testpaths = tests
addopts = -ra --strict-markers -p no:cacheprovider
asyncio_mode = auto
markers =
    integration: tests that start real MCP transports or subprocesses
    live_ollama: optional tests requiring a running Ollama service
